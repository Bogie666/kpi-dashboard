import SetupWizard from '@/components/setup/SetupWizard';

export const metadata = {
  title: 'Setup - KPI Dashboard',
  description: 'Set up your ServiceTitan KPI Dashboard',
};

export default function SetupPage() {
  return <SetupWizard />;
}
